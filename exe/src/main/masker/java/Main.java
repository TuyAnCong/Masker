import javagame.*;
import ytdl.*;
import rhythm.*;
public class MainMenu {
  title.map[title].string("Main Menu")
    playButton = rhythmAdd("Play", button)
    hp = rhythmAddHP(hpMax, hp)
    addButtomCard.href("card.ppf")
    addButtonCard.create.href("card.ppf", playButton)
 
    card = importLib("ytdl", "java:card", "ppf")
    c = card.ppf.Create(card.ytdl.thumbnail(RANDOM), card.ytdl.randomName(thumbnail))
    card.java:card.download(c)
                     
}
